# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build cache / test artifacts
*.test
*.out
/vendor/

# Compiled example binaries (go build in-place names them after the dir)
/examples/quickstart/quickstart
/examples/story/story
/examples/dnd/dnd
/examples/storyapi/storyapi
/examples/conexus-loom/conexus-loom

# Stray facade-generator binary if someone runs `go build ./internal/tools/facadegen`
# (go generate uses `go run`, so this is never produced by the normal flow)
/facadegen

# Local scratch / planning docs, not part of the library
/LOOM_*.md

# Stray local copy of the loom-ui playground (it lives in its own repo)
/examples/loom-ui/

# Environment / secrets
.env
.env.*
*.pem
*.key

# Docker volumes (local data)
loom_pgdata/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
*~
